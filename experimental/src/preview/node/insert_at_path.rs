use std::{
    path::Path
};

use crate::{
    Result
};

use super::{
    PreviewNode
};

impl PreviewNode {
    pub fn insert_at_path(&mut self, target_parent_path: &Path, node: &PreviewNode) -> Result<()> {
        unimplemented!()
    }
}