mod error;
mod path;

mod node;
mod item;
mod operation;
mod preview;

fn main() {
    println!("Hello, world!");
}

//First get the read operation iso
//Then the write
//Then think about a new layer of operations with user feedback